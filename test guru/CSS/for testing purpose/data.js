// Define some data
export const data = {
    name: "John",
    age: 30,
    city: "New York"
};

// You can also export functions, classes, or anything else
export function greet() {
    return `Hello, ${data.name}!`;
}
